// Ten layout jest pusty - główny PanelSidebar z /panel/layout.tsx obsługuje całą nawigację
export default function ShopPanelLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}